require "rom"
require "rom-sql"
require 'rom/sql/rake_task'

DB_PATH = File.join("sqlite://", "db", "escritorio")

namespace :db do
  task :setup do
    ROM::Configuration.new(:sql, DB_PATH)
  end
end
