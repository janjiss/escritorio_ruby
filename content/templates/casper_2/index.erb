<header class="main-header <%= "no-cover" unless blog.cover %>" <% if blog.cover %> style="background-image: url(<%= blog.cover %>)" <% end %>>
  <nav class="main-nav overlay clearfix">
    <% if blog.logo %><a class="blog-logo" href="<%= blog.url %>"><img src="<%= blog.logo %>" alt="<%= blog.title %>" /></a> <% end %>
    <% if blog.navigation %>
      <a class="menu-button icon-menu" href="#"><span class="word">Menu</span></a>
    <% end %>
  </nav>
  <div class="vertical">
    <div class="main-header-content inner">
      <h1 class="page-title"><%= blog.title %></h1>
      <h2 class="page-description"><%= blog.description %></h2>
    </div>
  </div>
  <a class="scroll-down icon-arrow-left" href="#content" data-offset="-45"><span class="hidden">Scroll Down</span></a>
</header>

<main id="content" class="content" role="main">

<%= partial "partials/loop", locals %>

</main>
